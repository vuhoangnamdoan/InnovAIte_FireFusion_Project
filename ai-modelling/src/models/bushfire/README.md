# Bushfire Modelling

This module contains the code for the bushfire modelling pipeline.

## Overview

The bushfire modelling pipeline is a pipeline that takes in a dataset of bushfire data and outputs a model that can predict the risk of a bushfire.

## Pipeline

The pipeline is a pipeline that takes in a dataset of bushfire data and outputs a model that can predict the risk of a bushfire.

## Time-series forecasting pipeline

The time-series forecasting pipeline is a pipeline that takes in a dataset of bushfire data and outputs a model that can predict the risk of a bushfire.
[Replace]

## Risk classifier

The risk classifier is a model that takes in a dataset of bushfire data and outputs a model that can predict the risk of a bushfire.
[Replace]